apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  # El nombre de nuestra nueva aplicación.
  name: common-resources
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Ml-For-Academic-Data/egi-infrastructure-k8s.git
    # ¡Punto Clave! Apunta directamente al chart que contiene el PVC.
    path: charts/common
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    # Los recursos comunes se crearán en el namespace 'desarrollo'.
    # Si tienes recursos comunes para otros ambientes, crearías apps similares
    # apuntando a otros namespaces.
    namespace: desarrollo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
